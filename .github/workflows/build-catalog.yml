name: Build and Deploy Catalog

on:
  workflow_dispatch: # Allows manual triggering from Actions tab or external API

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Fetch, Package Fonts, Build Catalog & Sync Discussions
        env:
          GITHUB_TOKEN: ${{ secrets.RATINGS_BOT_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          python tools/fetch_new_fonts.py
          python tools/package_fonts.py
          python tools/build_catalog.py
          python tools/sync_discussions.py

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
